!ЗАДАНИЕ: todo - localStorage - step - 1

HTML:

- input
    - button "Добавить"
        - ul

Нужно:

1. Создать массив todos.
   При загрузке страницы:

- взять todos из localStorage;
- если там ничего нет — использовать пустой массив.

2. При клике на кнопку:

- взять текст из input;
- если пусто — ничего не делать;
- создать объект задачи:

{
id: Date.now(),
text: "...",
completed: false
}

- добавить объект в массив todos;
- сохранить массив в localStorage;
- очистить input;
- вывести задачи в ul.

3. Каждая задача должна отображаться как li с текстом задачи.

Пока БЕЗ удаления и БЕЗ completed.
!ЗАДАНИЕ: todo - localStorage - step - 2

Продолжи предыдущее приложение.

===============================
НОВЫЙ ФУНКЦИОНАЛ
===============================

Возле каждой задачи должна появиться кнопка "Delete".

Пример:

[ ] Купить молоко [Delete]
[ ] Позвонить маме [Delete]

===============================
ПРИ НАЖАТИИ НА DELETE
===============================

Нужно:

1. Определить, по какой кнопке нажали.

2. Понять, какую именно задачу нужно удалить.

3. Удалить её из массива todos.

4. Сохранить обновлённый массив в localStorage.

5. Перерисовать список.

===============================
ОГРАНИЧЕНИЯ
===============================

- Не использовать циклы for.
- Использовать методы массивов.
- Не создавать новый обработчик для каждой кнопки.
- Использовать делегирование событий.

===============================
ПОДУМАЙ САМА
===============================

Подумай:

- Где лучше хранить id задачи?
- Как получить этот id после клика?
- Как удалить объект из массива по id?

Не ищи готовое решение.
Попробуй построить алгоритм самостоятельно.

! ЗАДАНИЕ: todo-localStorage-step-3

Продолжи предыдущее приложение.

===============================
НОВЫЙ ФУНКЦИОНАЛ
===============================

Каждую задачу можно отметить как выполненную.

===============================
ПРИ КЛИКЕ ПО ТЕКСТУ ЗАДАЧИ
===============================

Нужно:

1. Определить, по какой задаче кликнули.

2. Найти соответствующий объект в массиве todos.

3. Изменить свойство:

    completed: false

    на

    completed: true

    и наоборот.

4. Сохранить обновлённый массив в localStorage.

5. Перерисовать список.

===============================
ОТОБРАЖЕНИЕ
===============================

Если completed === true:

- добавить элементу класс "completed".

Если completed === false:

- класс не добавлять.

В CSS самостоятельно сделай:

.completed {
text-decoration: line-through;
}

После обновления страницы состояние задач
должно сохраняться.

===============================
ОГРАНИЧЕНИЯ
===============================

- Не создавать обработчик для каждой задачи.
- Использовать делегирование событий.
- Не менять completed напрямую через DOM.
- Источник истины — массив todos.

===============================
ПОДУМАЙ САМА
===============================

Подумай:

- На какой элемент лучше повесить обработчик?
- По чему удобнее кликать: по <li>, <p> или чему-то ещё?
- Как не перепутать клик по кнопке Delete и клик по тексту задачи?

Не смотри готовые решения.
Сначала построй алгоритм сама.

# /

# TODO LIST — STEP 4

НОВАЯ ФУНКЦИОНАЛЬНОСТЬ

Добавь кнопку:

"Delete completed"

---

## ПРИ НАЖАТИИ НА КНОПКУ

Нужно:

1. Удалить все задачи, у которых:

    completed === true

2. Сохранить обновлённый массив в localStorage.

3. Перерисовать список.

---

## ОГРАНИЧЕНИЯ

- Не использовать циклы for.
- Не удалять задачи по одной через splice().
- Подумай, какой метод массива подходит лучше.

---

## ПОДУМАЙ САМА

1. Как получить новый массив без выполненных задач?

2. Нужно ли искать индексы?

3. После удаления что нужно вызвать:
    - renderList()
    - saveTodos()
    - или оба?

---

## БОНУС (не обязательно)

Если выполненных задач нет,
кнопка не должна вызывать ошибок.

=========================================
/
=========================================
TODO LIST — STEP 5
=========================================

НОВАЯ ФУНКЦИОНАЛЬНОСТЬ

Добавь блок со статистикой задач.

Например:

Всего: 8
Активных: 5
Выполненных: 3

=========================================
ЧТО НУЖНО СДЕЛАТЬ
=========================================

1. Создать три элемента для отображения статистики.

2. После каждого изменения списка
   обновлять значения:

- Всего задач.
- Активных задач.
- Выполненных задач.

3. После перезагрузки страницы
   статистика должна отображаться правильно.

=========================================
ОГРАНИЧЕНИЯ
=========================================

- Не хранить количество задач
  в отдельных переменных.

- Не увеличивать и не уменьшать счётчики вручную.

- Статистика всегда должна вычисляться
  из массива todos.

=========================================
ПОДУМАЙ САМА
=========================================

1. Какая функция должна отвечать
   за обновление статистики?

2. Какие методы массива лучше использовать?

3. Нужно ли вызывать обновление статистики
   после каждого действия пользователя?

=========================================
БОНУС (не обязательно)
=========================================

Если задач нет, должно отображаться:

Всего: 0
Активных: 0
Выполненных: 0

без ошибок.

# /\*------------------------------------------------

# TODO LIST — STEP 6

НОВАЯ ФУНКЦИОНАЛЬНОСТЬ

Добавь три кнопки:

- All
- Active
- Completed

=========================================
ЧТО ДОЛЖНО ПРОИСХОДИТЬ
=========================================

При нажатии:

All

- показать все задачи.

Active

- показать только задачи,
  у которых completed === false.

Completed

- показать только задачи,
  у которых completed === true.

=========================================
ВАЖНО
=========================================

Фильтрация должна менять
ТОЛЬКО отображение списка.

Массив todos изменять нельзя.

После переключения фильтра:

- задачи не должны удаляться;
- completed не должен изменяться;
- localStorage менять не нужно.

=========================================
ОГРАНИЧЕНИЯ
=========================================

- Не создавать отдельный массив,
  который будет хранить активные
  или выполненные задачи.

- Не использовать splice().

- Использовать методы массива.

=========================================
ПОДУМАЙ САМА
=========================================

1. Что лучше передавать в renderList():
   весь массив или тот, который нужно показать?

2. Нужно ли renderList() всегда использовать
   глобальный todos?

3. Как сделать так, чтобы один renderList()
   умел отображать любые массивы?

=========================================
БОНУС (не обязательно)
=========================================

Сделай активную кнопку фильтра
визуально выделенной.

Например:

All Active Completed

↓

[All] Active Completed

или

All [Active] Completed
\
//\*
=========================================
TODO LIST — STEP 7
=========================================

НОВАЯ ФУНКЦИОНАЛЬНОСТЬ

Добавь возможность редактировать текст задачи
через отдельные кнопки.

У каждой задачи должны быть кнопки:

- Mark as complete / Mark as active
- Edit
- Delete

=========================================
РЕЖИМ РЕДАКТИРОВАНИЯ
=========================================

При нажатии на кнопку Edit:

1. Скрыть обычный текст задачи.

2. Показать input.

3. В input должен находиться
   текущий текст задачи.

4. Показать кнопку Save changes.

Редактироваться должна только та задача,
возле которой нажали Edit.

=========================================
СОХРАНЕНИЕ ИЗМЕНЕНИЙ
=========================================

При нажатии на кнопку Save changes:

1. Получить новое значение из input.

2. Найти соответствующую задачу
   в массиве todos по id.

3. Изменить её свойство text.

4. Сохранить обновлённый массив
   в localStorage.

5. Перерисовать список.

=========================================
ПУСТОЕ ЗНАЧЕНИЕ
=========================================

Если пользователь оставил input пустым
или ввёл только пробелы:

- не сохранять пустое значение;
- оставить старый текст задачи.

=========================================
ВАЖНО
=========================================

- Не использовать двойной клик.
- Не использовать клик по тексту задачи
  для переключения completed.
- Для каждого действия использовать
  отдельную кнопку.
- Использовать один обработчик на списке
  через делегирование событий.
- Действие кнопки определять через data-btn.
- Источник истины — массив todos.
- После перезагрузки изменённый текст
  должен сохраниться.

=========================================
ПОДУМАЙ САМА
=========================================

1. Как определить id редактируемой задачи?

2. Как получить input именно внутри
   нужного li?

3. Как отличить кнопку Edit
   от кнопки Save changes?

4. Как найти задачу в todos по id?

5. В какой момент вызывать:

    saveTodos()
    renderList()

=========================================
БОНУС (не обязательно)
=========================================

Добавь кнопку Cancel.

При нажатии на Cancel:

- не сохранять изменения;
- выйти из режима редактирования;
- снова показать старый текст задачи.
  \*/

# /\*

# TODO LIST — STEP 8

Добавь отображение даты создания задачи.

=========================================
НОВОЕ СВОЙСТВО
=========================================

При создании задачи сохрани дату.

Подумай сама:

- в каком формате её хранить;
- строкой или числом;
- чтобы потом можно было удобно вывести.

=========================================
ОТОБРАЖЕНИЕ
=========================================

Под каждой задачей показать дату создания.

Например:

---

Task 3

Buy milk

Created:
13.07.2026 22:41

---

=========================================
ТРЕБОВАНИЯ
=========================================

- Дата должна сохраняться
  в localStorage.

- После перезагрузки страницы
  дата не должна меняться.

- У каждой задачи должна быть
  своя дата создания.

=========================================
БОНУС 1
=========================================

Сделай красивый формат даты.

Например:

13.07.2026
22:41

или

13 Jul 2026
22:41

или любой другой читаемый вариант.

=========================================
БОНУС 2
=========================================

Если задача была отредактирована,
добавь ещё одно поле.

Edited:
13.07.2026 23:10

Оно должно появляться
только после первого редактирования.

Если задача ни разу не редактировалась,
ничего не показывать.

=========================================
ОГРАНИЧЕНИЯ
=========================================

- Не использовать сторонние библиотеки.
- Использовать встроенный объект Date.
- Не изменять дату создания
  при редактировании задачи.
- После обновления страницы
  обе даты должны сохраняться.
  /\*
  =========================================
  TODO LIST — STEP 9
  =========================================

Добавь подтверждение удаления задачи.

=========================================
ЧТО ДОЛЖНО ПРОИСХОДИТЬ
=========================================

При нажатии на кнопку Delete:

1. Показать пользователю окно подтверждения.

Текст, например:

"Delete this task?"

2. Если пользователь нажал OK:

- удалить задачу;
- сохранить обновлённый массив в localStorage;
- перерисовать список.

3. Если пользователь нажал Cancel:

- ничего не удалять;
- не сохранять localStorage;
- не вызывать renderList().

=========================================
ТРЕБОВАНИЯ
=========================================

- Использовать встроенный confirm().
- Не менять остальную логику удаления.
- Подтверждение должно работать
  только для удаления одной задачи.
- Кнопка Delete completed пока работает
  без подтверждения.

=========================================
ПОДУМАЙ САМА
=========================================

1. Что возвращает confirm()?

2. Где лучше вызвать confirm():
   до удаления или после?

3. В какой момент нужно делать:

    saveTodos()
    renderList()

4. Что должно произойти,
   если пользователь нажал Cancel?

=========================================
БОНУС
=========================================

В тексте подтверждения покажи
название удаляемой задачи.

Например:

"Delete task: Buy milk?"
\*/

# /\*

# TODO LIST — STEP 10

Добавь поиск по тексту задач.

=========================================
ИНТЕРФЕЙС
=========================================

Добавь:

- input для поиска;
- кнопку Search;
- кнопку Reset.

=========================================
ПОИСК
=========================================

При нажатии Search:

1. Получить текст из поискового input.

2. Найти задачи, текст которых содержит
   введённое значение.

3. Поиск должен быть без учёта регистра.

Например:

"Buy milk"

должно находиться по запросам:

"buy"
"MILK"
"milk"

4. Показать только найденные задачи.

=========================================
ВАЖНО
=========================================

- Не изменять массив todos.
- Не сохранять результаты поиска
  в localStorage.
- Не делать новый массив частью
  постоянного состояния приложения.
- Поиск меняет только отображение.

=========================================
ЕСЛИ НИЧЕГО НЕ НАЙДЕНО
=========================================

Показать сообщение:

"No tasks found."

=========================================
КНОПКА RESET
=========================================

При нажатии Reset:

- очистить поисковый input;
- снова показать задачи
  с учётом текущего фильтра:

    All / Active / Completed.

=========================================
ТРЕБОВАНИЯ
=========================================

- Использовать form и событие submit,
  чтобы поиск работал и по кнопке,
  и по Enter.

- Использовать preventDefault().

- Использовать filter().

- Не делать повторный renderHTML-код.

=========================================
ПОДУМАЙ САМА
=========================================

1. Как совместить поиск
   с currentFilter?

2. Что должно произойти,
   если выбран фильтр Active,
   а затем выполнен поиск?

3. Нужно ли хранить поисковую строку
   в отдельной переменной состояния?

4. Как вернуть обычное отображение
   после Reset?
   \*/
